package com.bcttgd.parentapp;

public class User {
    public static boolean authenticate(String username, String password) {
        // Authenticate the user using the credentials provided
        // Return true if the user is authenticated, false otherwise
        // TODO : Actually login the user
        return true;
    }

    public static boolean createUser(String name, String mail, String password) {
        // Create a new user with the name, mail and password provided
        // Return true if the user is created, false otherwise
        // TODO : Actually create the user
        return true;
    }

    public static boolean logout() {
    	// Logout the current user
    	// Return true if the user is logged out, false otherwise
    	// TODO : Actually logout the user
    	return true;
    }
}
